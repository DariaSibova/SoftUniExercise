function solve(input){
    let whether = input[0];
    if(whether == "sunny"){
        console.log("It's warm outside!")
    }else {
        console.log("It's cold outside!")
    }
}
solve(["sunny"])
// solve(["cloudy"])
// solve(["snowy"])
